# Multiagent Consensus - Base Environment Variables
# ==============================================
# Public variables used across all environments
# These values are safe to commit to version control

# Application Settings
# -------------------
# Debug mode (false by default, can be overridden in environment-specific configs)
DEBUG=false

# Default API URL (can be overridden in environment-specific configs)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Default deployment options
DEFAULT_TIMEOUT_MS=30000
DEFAULT_MAX_RETRIES=3 